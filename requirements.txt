# requirements.txt — Baymax
# Phase 2 : Vision
opencv-python-headless>=4.9
mediapipe>=0.10
numpy>=1.26

# Phase 3 : Moteurs (décommenter quand nécessaire)
# pyserial>=3.5

# Phase 5 : OLED (décommenter quand nécessaire)
# luma.oled>=3.13
# Pillow>=10.0

# Phase 7 : Audio (décommenter quand nécessaire)
# vosk>=0.3
# pyaudio>=0.2
# pyttsx3>=2.90

# Phase 8 : LLM (décommenter quand nécessaire)
# ollama>=0.3
# openai>=1.30
